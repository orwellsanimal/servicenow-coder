{
    "name": "{{packageName}}",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "catalog:",
        "@servicenow/glide": "catalog:",
        "typescript": "catalog:",
        "@servicenow/eslint-plugin-sdk-app-plugin": "catalog:"
    }
}
