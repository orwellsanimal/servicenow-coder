/**
 * UI Action: {{actionName}}
 * Table: {{tableName}}
 * Form/List: Form
 * Show Insert: true
 * Show Update: true
 *
 * {{description}}
 *
 * Available objects:
 *   current - GlideRecord of the current record
 *   action  - GlideActionURL for redirect control
 *   gs      - GlideSystem utilities
 */

// Guard: check conditions
// if (current.state != 'new') {
//     gs.addErrorMessage('Action only available for new records.');
//     action.setRedirectURL(current);
//     return;
// }

// Perform the action
// current.setValue('state', 'active');
// current.update();

// Redirect back to the record
// gs.addInfoMessage('Action completed successfully.');
// action.setRedirectURL(current);
